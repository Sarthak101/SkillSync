Add Register Page
Add Regiser Button
Put Src in buttons

Register Page (SignUP)

Make sure all the links work


--Changing the double login to single login (the page will automatically detect which login it is)
-- make the page more approachable for people who need to get work done (put the employee login a bit hidden)